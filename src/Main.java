import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args){

        MainFrame mf = new MainFrame("Cat", new Dimension(300,400));
        mf.init();
        /*JFrame frame = new JFrame("Penetration testing");

        frame.setSize(900,500);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLocationRelativeTo(null);


        frame.setLayout(new GridBagLayout());
        JButton mB = new JButton("???");
        JButton yB = new JButton("!!!");

        JLabel label = new JLabel("Введите ФИО");
        JTextField textField = new JTextField(20);
        JLabel label2 = new JLabel("Введите пароль");
        JPasswordField passwordField = new JPasswordField(20);
        JButton button = new JButton("Отправить");
        JProgressBar progress = new JProgressBar();
        progress.setStringPainted(true);
        progress.setMinimum(0);
        progress.setMaximum(100);
        frame.add(progress);

        frame.add(label);
        frame.add(textField);
        frame.add(label2);
        frame.add(passwordField);
        frame.add(button);

        frame.add(mB);
        frame.add(yB);

        frame.setVisible(true);

        for (int i = progress.getMinimum(); i < progress.getMaximum(); i++) {
            Thread.sleep(500);
            progress.setValue(i);
        }


*/


    }
}
